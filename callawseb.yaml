apiVersion: v1
kind: Pod
metadata:
  name: command-demo
  labels:
    purpose: demonstrate-command
spec:
  containers:
  - name: command-demo-container
    image: centos
    command: ["/bin/sh"]
    args: ["-c", "while true; do curl http://test-env.eba-g3z922cv.us-east-1.elasticbeanstalk.com/; sleep 10;done"]
  restartPolicy: OnFailure
